# Skyway 生产环境配置（Docker 外挂）
# 使用方式：复制为 application-prod.yml 并按实际环境修改
# 容器启动参数已包含: --spring.config.additional-location=file:/app/config/

server:
  port: 8080

skyway:
  # 上传文件路径（容器内挂载卷，如 /app/upload）
  profile: /app/upload

spring:
  redis:
    host: redis
    port: 6380
    password: ${REDIS_PASSWORD:J98%FHF#9h@e88h9fre9}
    database: 0
    timeout: 10s
  datasource:
    druid:
      master:
        url: jdbc:mysql://mysql:3307/${MYSQL_DATABASE:-skyway}?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
        username: ${MYSQL_USER:-skyway}
        password: ${MYSQL_PASSWORD:o5KjnPYxswsiURZHCPwd}
